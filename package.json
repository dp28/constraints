{
  "name": "constraints",
  "version": "0.0.0",
  "author": "",
  "main": "app/build/index.js",
  "dependencies": {
    "bluebird": "^3.4.6"
  },
  "devDependencies": {
    "mocha": "*",
    "run-sequence": "^1.1.1",
    "tslint": "^3.15.1",
    "typescript": "^2.0.3",
    "typings": "^1.4.0"
  },
  "scripts": {
    "compile": "tsc --pretty",
    "compile:watch": "tsc --pretty -w",
    "test": "tsc --pretty && mocha",
    "lint": "tslint 'app/src/**/*.ts' --force"
  }
}
